# WeldSnap V2.0 端到端测试计划

> [!WARNING]
> 本文档是 V1 或迁移过程中的历史资料，不是当前运行规范。当前实现以 `src/`、`package.json` 和 `.github/workflows/deploy.yml` 为准；其中旧的 Express、本地 `exports/`、旧会话或旧 OSS 路径说明仅供追溯。


> **目标**：验证三层角色（系统管理员 / 项目管理员 / 施工人员）下所有已开发功能的正确性与权限隔离。
>
> **测试环境**：本地 `pnpm dev`，浏览器（桌面 Chrome + 手机模式 Chrome DevTools），已运行 `scripts/mock-data.js` 生成 3 个测试项目。
>
> **测试账号**：
>
> | 账号 | 角色 | 密码 | 用途 |
> |------|------|------|------|
> | `admin` | 系统管理员 | `admin123` | 内置固有账户 |
> | `subadmin` | 项目管理员 | （自设） | 验证项目管理员权限边界 |
> | `worker01` | 施工人员 | （自设） | 验证施工人员权限边界 |
> | 匿名设备 | 简易登录 | — | 验证匿名登录流程 |

---

## 一、系统管理员（admin）全功能测试

### 1.1 登录与导航

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.1.1 | 密码登录 | 输入 `admin` / `admin123`，点击"管理登录" | 成功后跳转 `/admin`，顶栏显示"系统管理员"标签 |
| 1.1.2 | 未登录拦截 | 清除 Cookie 后直接访问 `/admin` | 重定向到 `/login` |
| 1.1.3 | Tab 导航 | 检查顶部 Tab | 可见"管道焊口总览"、"成员管理"、"系统设置"三个 Tab |
| 1.1.4 | 退出登录 | 点击顶栏"退出" | 跳转 `/login`，Cookie 清除 |

### 1.2 项目管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.2.1 | 查看项目列表 | 进入"管道焊口总览"Tab | 显示 3 个测试项目，按创建时间倒序，名称含"测试项目" |
| 1.2.2 | 按施工号/名称过滤 | 在搜索框输入施工号关键字 | 列表实时过滤，仅显示匹配项 |
| 1.2.3 | 新建项目 | 点击"新建项目"，填写施工号 `SG-TEST-99`、项目名称 `测试项目-E2E新建`、管线前缀 `TST`、焊口前缀 `TW` | 创建成功，列表刷新出现新项目 |
| 1.2.4 | 编辑项目 | 点击刚创建项目的"编辑"，修改项目名称为 `测试项目-E2E已编辑` | 保存成功，名称更新 |
| 1.2.5 | 删除项目（无数据） | 删除刚创建的空项目 | 删除成功，列表刷新 |
| 1.2.6 | 项目详情进入 | 点击某项目的施工号 | 列表收起，面包屑显示"项目控制台 / 🏗️ 施工号: XXX"，右侧显示管线树和焊口矩阵 |

### 1.3 管线管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.3.1 | 查看管线列表 | 进入某项目后，左侧管线树 | 显示该项目下所有管线，按名称排序，右侧显示完成度 `x/y` |
| 1.3.2 | 自动生成管线 | 项目有前缀时，点击"自动生成管线"按钮 | 新管线按 `PL-XXX` 格式自增生成（3 位补零） |
| 1.3.3 | 手动新增管线 | 项目无前缀时，点击“添加管线”，在弹出的提示框中输入管线号，点击“确定” | 新管线出现在列表中 |
| 1.3.4 | 管线号筛选 | 在管线搜索框输入关键字 | 列表实时过滤 |
| 1.3.5 | 编辑管线号 | 双击某管线号文本 → 修改 → Enter 保存 | 管线号更新，刷新后仍保持 |
| 1.3.6 | 编辑管线号（重名） | 双击修改为已存在的管线号 | 弹窗提示"已存在"，不保存 |
| 1.3.7 | 编辑取消 | 双击编辑后按 Esc | 恢复原值，不发送请求 |
| 1.3.8 | 单个二维码查看 | 点击管线号旁的"QR"按钮 | 弹窗显示该管线二维码图片 |
| 1.3.9 | 批量二维码打印 | 勾选若干管线 → 点击"打印二维码" | 新窗口打开打印页，仅显示已勾选管线的二维码 |
| 1.3.10 | 全量二维码打印 | 不勾选任何管线 → 点击"打印二维码" | 新窗口打开打印页，显示项目下全部管线二维码 |
| 1.3.11 | 二维码打印分页 | 管线数量较多时打印 | 二维码卡片不被跨页切断（`page-break-inside: avoid` 生效） |
| 1.3.12 | 删除管线（无照片） | 勾选不含照片的管线 → 删除 | 删除成功，关联焊口级联删除 |
| 1.3.13 | 删除管线（含照片，熔断） | 勾选含照片的管线 → 删除 | 弹窗提示"已包含照片记录...本次批量操作已拦截" |
| 1.3.14 | 强制删除管线（管理员确认） | 熔断后点击确认强删 | 二次确认弹窗 → 确认后删除成功，照片记录一并清除 |

### 1.4 焊口管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.4.1 | 查看焊口列表 | 选中某管线，右侧矩阵 | 显示该管线下所有焊口，列：焊口号、组对/打底/盖面工序、上传人、上传时间 |
| 1.4.2 | 自动生成焊口 | 项目有前缀时，点击"自动生成焊口" | 新焊口按 `W-XX` 格式自增生成（2 位补零） |
| 1.4.3 | 手动新增焊口 | 项目无前缀时，输入焊口号，点击"添加" | 新焊口出现在矩阵中 |
| 1.4.4 | 编辑焊口号 | 双击某焊口号文本 → 修改 → Enter 保存 | 焊口号更新 |
| 1.4.5 | 编辑焊口号（重名） | 双击修改为同管线已存在的焊口号 | 弹窗提示"已存在"，不保存 |
| 1.4.6 | 现场创建标识 | 查看匿名用户创建的焊口 | 显示黄色"现场创建" Tag |
| 1.4.7 | 批量选择操作 | 勾选若干焊口 | 顶部出现"批量下载"和"删除已选"按钮，显示已选数量 |
| 1.4.8 | 全选/清空/反选 | 分别点击"全选"、"清空"、"反选" | 复选框状态正确联动，多选指示词为“清空” |
| 1.4.9 | 删除焊口（无照片） | 勾选不含照片的焊口 → 删除 | 删除成功 |
| 1.4.10 | 删除焊口（含照片，熔断） | 勾选含照片的焊口 → 删除 | 熔断拦截，提示需系统管理员确认 |
| 1.4.11 | 强制删除焊口（管理员确认） | 熔断后确认强删 | 删除成功 |

### 1.5 工序照片上传（网页端）

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.5.1 | 点击"未上传"触发上传 | 点击某工序的"未上传"标签 | 弹出文件选择器（仅接受图片） |
| 1.5.2 | 选择照片上传 | 选择一张手机照片 | 流程：本地压缩 → 获取签名 → 直传 OSS → 确认写入 DB → 矩阵刷新显示"已上传" |
| 1.5.3 | 上传进度提示 | 上传过程中观察 | 依次显示：压缩中 → 获取授权 → 上传中 → 写入数据库 → 上传成功 |
| 1.5.4 | 上传失败处理 | 断网后尝试上传 | 显示红色错误提示，含错误原因 |
| 1.5.5 | 覆盖已上传照片 | 管理员点击"已上传"标签进入详情 → "重新上传覆盖" | 旧照片被新照片替换 |

### 1.6 工序照片预览与管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.6.1 | 悬浮预览 | 鼠标悬停在"已上传"标签上 | 跟随鼠标的缩略图气泡出现 |
| 1.6.2 | 大图查看 | 点击"已上传"标签 | 弹出大图 Modal，显示管线号、焊口号、工序类型 |
| 1.6.3 | 标记不合格 | 在大图 Modal 中点击"标记不合格 (需重传)" | 确认后照片状态变为红色闪烁"需重传" |
| 1.6.4 | 不合格照片预览 | 悬停在"需重传"标签上 | 仍可预览原照片（`REJECTED:` 前缀自动剥离） |
| 1.6.5 | 施工端联动 | 标记不合格后，以施工人员身份查看该焊口 | 对应工序卡片红色高亮 + 显示历史不合格照片对比图 |
| 1.6.6 | 保存下载单张 | 在大图 Modal 中点击"保存下载图片" | 浏览器下载照片，文件名格式：`管线号_焊口号_工序.jpg` |

### 1.7 批量下载

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.7.1 | 批量下载已选焊口 | 勾选若干焊口 → 点击"批量下载已选" | 浏览器并发拉取 OSS 图片 → 客户端打包 ZIP → 自动下载。文件名格式：`施工号_焊口照片归档_日期.zip` |
| 1.7.2 | 批量下载进度 | 下载过程中观察 | 显示进度："正在下载打包 (x/y)..." |
| 1.7.3 | 空选择下载 | 不勾选任何焊口 → 点击下载 | 提示"请先勾选" |
| 1.7.4 | 无照片下载 | 勾选全部未上传的焊口 → 下载 | 提示"尚无有效照片记录" |
| 1.7.5 | 自动过滤空工序 | 勾选混合包含已上传和未上传的焊口 → 下载 | 仅下载已上传照片，未上传工序自动过滤不中断流程 |

### 1.8 导入/导出

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.8.1 | 下载导入模板 | 在导入弹窗中点击"下载导入模板" | 下载 Excel，含两列：`管线号`、`焊口号`，一行示例数据 |
| 1.8.2 | 导出当前项目数据 | 在导入弹窗中点击"导出当前项目数据" | 下载 Excel，含三列：`管线号`、`焊口号`、`创建来源`，包含该项目全部数据 |
| 1.8.3 | 导入新建 | 上传含新管线号+焊口号的 Excel | 新增成功，统计"新增: N，跳过: 0" |
| 1.8.4 | 导入跳过重复 | 上传与已有数据重复 of Excel | 全部跳过，统计"新增: 0，跳过: N" |
| 1.8.5 | 导入合并管线 | 上传已有管线号但新焊口号的 Excel | 新焊口追加到已有管线下，不新建管线 |
| 1.8.6 | 导入空文件 | 上传空 Excel | 提示"Excel 文件内容为空" |
| 1.8.7 | 导入缺少列 | 上传缺少"焊口号"列的 Excel | 提示"缺少必需的列" |

### 1.9 统计看板（工序级）

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.9.1 | 焊口总量 | 查看顶部 StatsBar "焊口总数" | 数值 = 该项目下所有焊口记录数 |
| 1.9.2 | 已完成工序数 | 查看"已完成工序数" | 数值 = 该项目下所有已成功上传的工序（组对/打底/盖面）合格照片数累加 |
| 1.9.3 | 待录入工序数 | 查看"待录入工序数" | 数值 = (焊口总数 * 3) - 已完成工序数 |
| 1.9.4 | 质量记录完成进度 | 查看完成进度百分比及进度条 | 百分比 = 已完成工序数 / (焊口总数 * 3) × 100%，与计算相符 |
| 1.9.5 | 上传后实时刷新 | 上传一张照片后观察 | 已完成工序数 +1，待录入工序数 -1，进度条比值变化 |
| 1.9.6 | 驳回后统计变化 | 标记某照片不合格后观察 | 已完成工序数 -1，待录入工序数 +1 |

### 1.10 成员管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.10.1 | 查看成员列表 | 切换到"成员管理" Tab | 显示所有用户，含角色标签（系统管理员/项目管理员/施工员） |
| 1.10.2 | 添加项目管理员 | 点击"添加新成员"，角色选"项目管理员" | 创建成功，列表刷新 |
| 1.10.3 | 添加施工人员 | 角色选"施工人员" | 创建成功 |
| 1.10.4 | 编辑用户 | 点击某用户的"编辑"，修改姓名 | 保存成功 |
| 1.10.5 | 编辑简易账户用户名（禁用） | 编辑 `anon_` 开头的用户 | 用户名字段只读（disabled） |
| 1.10.6 | 删除用户 | 点击某用户"删除" | 删除成功 |
| 1.10.7 | 删除固有管理员 | 尝试删除 `admin` 用户 | 显示"固有账户"，无删除按钮 |
| 1.10.8 | 删除最后一个管理员 | 如果只有 1 个 admin，尝试删除 | 提示"不能删除最后一个管理员" |
| 1.10.9 | 90 天自动销号 | 手动修改某 `anon_` 用户的 `last_login_at` 为 100 天前 → 刷新列表 | 该用户被自动清除 |

### 1.11 系统设置

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 1.11.1 | 查看系统设置 | 切换到"系统设置" Tab | 显示 OSS 配置（脱敏）、服务器 IP、端口 |
| 1.11.2 | OSS 信息脱敏 | 检查 AccessKeyId | 显示前 6 位 + `***` + 后 4 位 |

---

## 二、项目管理员（subadmin）权限测试

> **核心验证点**：项目管理员能做系统管理员的项目/管线/焊口管理操作，但不能访问成员管理、系统设置，不能强删含照片记录。

### 2.1 登录与导航

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 2.1.1 | 密码登录 | 用 `subadmin` 账号登录 | 成功后跳转 `/admin`，顶栏显示"项目管理员"标签 |
| 2.1.2 | Tab 可见性 | 检查顶部 Tab | 仅可见"管道焊口总览"，**不可见**"成员管理"和"系统设置" |
| 2.1.3 | 直接访问成员管理 | 地址栏输入 `/admin` 并切换到 users Tab（或直接调 API） | Tab 不存在；API 调用返回 403 |

### 2.2 项目管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 2.2.1 | 新建项目 | 创建新项目 | 成功（权限与系统管理员一致） |
| 2.2.2 | 编辑项目 | 编辑已有项目 | 成功 |
| 2.2.3 | 删除空项目 | 删除无数据项目 | 成功 |

### 2.3 管线管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 2.3.1 | 新增管线 | 自动生成或手动添加 | 成功 |
| 2.3.2 | 编辑管线号 | 双击修改管线号 | 成功 |
| 2.3.3 | 二维码查看/打印 | 点击 QR 或批量打印 | 成功 |
| 2.3.4 | 导入 Excel | 上传包含新数据的 Excel | 成功 |
| 2.3.5 | 删除管线（无照片） | 删除不含照片的管线 | 成功 |
| 2.3.6 | 删除管线（含照片） | 删除含照片的管线 → 熔断 → 确认强删 | **熔断后提示"项目管理员无权删除包含照片的记录，请联系系统管理员处理"，无强删确认弹窗** |

### 2.4 焊口管理

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 2.4.1 | 新增焊口 | 自动生成或手动添加 | 成功，`create_source` 为"管理控制台创建"（非"现场创建"） |
| 2.4.2 | 编辑焊口号 | 双击修改焊口号 | 成功 |
| 2.4.3 | 上传照片 | 网页端上传工序照片 | 成功 |
| 2.4.4 | 预览/下载照片 | 悬停预览、大图查看、保存下载 | 成功 |
| 2.4.5 | 标记不合格 | 标记照片为不合格 | 成功 |
| 2.4.6 | 批量下载 | 勾选焊口批量下载 ZIP | 成功 |
| 2.4.7 | 删除焊口（无照片） | 删除不含照片的焊口 | 成功 |
| 2.4.8 | 删除焊口（含照片） | 删除含照片的焊口 → 熔断 | **提示无权强删，不提供确认选项** |

### 2.5 越权测试

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 2.5.1 | 调用成员管理 API | `POST /api/admin/users` | 返回 403 "需要系统管理员权限" |
| 2.5.2 | 调用系统设置 API | `GET /api/admin/settings` | 返回 403 "需要系统管理员权限" |
| 2.5.3 | 调用强删 API（force=true） | `POST /api/admin/records/bulk-delete` 带 `force: true` | 熔断拦截（`isSystemAdmin` 为 false），返回含照片记录的拦截提示 |
| 2.5.4 | 编辑用户信息 | `PUT /api/admin/users/[id]` | 返回 403 |
| 2.5.5 | 删除用户 | `DELETE /api/admin/users/[id]` | 返回 403 |

---

## 三、施工人员（worker）权限测试

> **核心验证点**：施工人员只能在移动端拍照，不能访问管理后台。

### 3.1 登录与导航

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 3.1.1 | 密码登录 | 用 `worker01` 账号登录 | 成功后跳转 `/upload`（非 `/admin`） |
| 3.1.2 | 直接访问管理台 | 地址栏输入 `/admin` | 重定向到 `/login` |

### 3.2 移动端上传

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 3.2.1 | 搜索管线号 | 在上传页搜索框输入管线号 | 显示匹配结果 |
| 3.2.2 | 选择焊口 | 点击某管线 → 展开焊口列表 | 显示焊口及工序状态 |
| 3.2.3 | 拍照上传 | 点击"未上传"工序 → 调起相机 → 拍照 | 压缩 → 直传 OSS → 确认，状态变为"已上传" |
| 3.2.4 | 需重传照片 | 查看被管理员标记不合格的工序 | 红色高亮 + 显示历史不合格照片对比 |
| 3.2.5 | 覆盖上传 | 施工人员只能上传空位，不能覆盖已上传 | 已上传工序不显示上传入口（管理员可覆盖，施工人员不可） |

### 3.3 越权测试

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 3.3.1 | 调用管理台 API | `GET /api/admin/projects` | 返回 403 "需要管理员权限" |
| 3.3.2 | 调用上传签名 API | `POST /api/upload/sign` | 成功（已登录即可上传） |

---

## 四、匿名/简易登录测试

### 4.1 简易登录流程

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 4.1.1 | 首次登录 | 清除 LocalStorage → 访问 `/login` → 输入姓名 → 点击"简易登录" | 自动生成设备 ID，创建 `anon_` 账户，跳转 `/upload` |
| 4.1.2 | 重名处理 | 用已有姓名再次从新设备登录 | 自动加后缀，如 `张师傅#0001` |
| 4.1.3 | 再次访问 | 同一设备再次访问 `/login` | 显示已保存的设备账号名，无需重新输入姓名 |
| 4.1.4 | 直接访问管理台 | 匿名用户地址栏输入 `/admin` | 重定向到 `/login` |

### 4.2 越权测试

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 4.2.1 | 调用管理台 API | `GET /api/admin/projects` | 返回 401 "请先登录"（session 为 worker 角色） |
| 4.2.2 | 调用成员管理 API | `GET /api/admin/users` | 返回 403 |

---

## 五、统计一致性交叉验证

> 在完成上述功能测试后，抽查以下场景的统计数值是否与实际一致。

| # | 场景 | 操作 | 验证方法 |
|---|------|------|---------|
| 5.1 | 新增焊口后 | 新增 1 个焊口 | StatsBar 焊口总量 +1，待录入工序数 +3，已完成工序数不变 |
| 5.2 | 上传一张照片后 | 上传某焊口的组对工序 | 总量不变，已完成工序数 +1，待录入工序数 -1 |
| 5.3 | 三道工序全上传后 | 上传完某焊口的打底+盖面 | 总量不变，已完成工序数 +2，待录入工序数 -2 |
| 5.4 | 标记不合格后 | 驳回某焊口的盖面工序 | 已完成工序数 -1，待录入工序数 +1 |
| 5.5 | 重新上传后 | 施工人员重传被驳回的照片 | 已完成工序数 +1，待录入工序数 -1 |
| 5.6 | 删除焊口后 | 删除 1 个带 1 张照片的焊口（强制删除） | 焊口总量 -1，已完成工序数 -1，待录入工序数 -2 |
| 5.7 | 批量导入后 | 导入含 5 条新记录的 Excel | 焊口总量 +5，待录入工序数 +15，已完成工序数不变 |
| 5.8 | 跨项目统计 | 切换项目 | 统计数字切换到对应项目，不混淆 |

---

## 六、响应式与移动端适配（及视觉一致性）

| # | 测试项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| 6.1 | 手机模式上传页 | Chrome DevTools 模拟 iPhone → 访问 `/upload` | 布局自适应，按钮足够大，可正常拍照上传 |
| 6.2 | 管理台窄屏 | 缩小浏览器窗口至 1024px | 左右分栏正常显示，不溢出 |
| 6.3 | 二维码打印页 | 窄屏下打开打印页 | 二维码卡片排列合理 |
| 6.4 | 左右栏目对称性 | 观察项目详情页面下，左右操作栏的高度与对齐情况 | 左右操作栏高度皆为固定的 `76px` 且底部边框线（border-bottom）完全齐平 |
| 6.5 | 按钮与指示词风格 | 观察“打印二维码”按钮样式与多选指示词 | “打印二维码”为符合 Carbon 规范的蓝边中空三级 button（文字为蓝色，背景透明），且多选选项中的“全清”已统一改名为“清空” |
